// Initialize AOS (Animate on Scroll)
AOS.init({
    duration: 1000, // Animation duration in milliseconds
    once: true, // Animations only run once
});

// Additional custom JavaScript can go here
console.log("Custom scripts loaded successfully!");

// Example: Add some interactivity or animations
document.addEventListener("DOMContentLoaded", function () {
    // Add any DOM-based interactions here
});

// Example: Alert on button click
document.querySelector(".btn-primary").addEventListener("click", function () {
    alert("Primary button clicked!");
});

